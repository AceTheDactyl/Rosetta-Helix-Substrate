name: K.I.R.A. Claude API Test

on:
  workflow_dispatch:
    inputs:
      message:
        description: 'Message to test'
        required: true
        default: 'Hello KIRA'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Simple Test
        run: |
          echo "Testing with message: ${{ github.event.inputs.message }}"
          echo "This workflow is working correctly"